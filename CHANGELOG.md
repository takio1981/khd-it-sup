# Changelog

บันทึกการเปลี่ยนแปลงของระบบ เรียงจากล่าสุดไปเก่าสุด — อัปเดตทุกครั้งที่มีการแก้ไขโค้ด (ดู
"Changelog + SOP workflow diagrams" ใน `CLAUDE.md`) ประวัติก่อนวันที่ 2026-08-30 ดูได้จาก `git log`

## 2026-09-04

- แก้บั๊กสัญลักษณ์ในผัง SOP กองรวมกันที่มุมบนซ้ายแทนที่จะอยู่ตำแหน่งจริงในผัง (เจอกับโหนด "ประวัติการใช้งานระบบ" ในผังภาพรวม) — ต้นเหตุคือ CSS `transform: scale()` ที่ใช้ทำแอนิเมชันตอนโหนดปรากฏ/ตอน hover ชนกับการจัดวาง `<foreignObject>` ของป้ายข้อความภายใน SVG (บั๊กเฉพาะของเบราว์เซอร์เวลาผสม CSS transform + transform-box:fill-box กับ foreignObject) แก้โดยตัดเอฟเฟกต์ขยาย/ย่อออก เหลือแค่จุดจาง-ชัด (opacity) และเงาตอน hover แทน ซึ่งปลอดภัยกว่า
- แก้บั๊กเมนูผัง SOP กระตุกตอนสลับแท็บ: mat-tab-group ไม่ได้โหลดเนื้อหาทุกแท็บพร้อมกันตอนเปิดหน้าแรก (สร้างจริงตอนกดแท็บนั้นครั้งแรกเท่านั้น) ทำให้แท็บที่ยังไม่เคยกดค้างมองไม่เห็นตลอด — เปลี่ยนไปใช้ MutationObserver เฝ้าดู DOM แทนการเติมแอนิเมชันครั้งเดียวตอนโหลดหน้า
- เพิ่มจุดกระพริบเคลื่อนที่ไปตามเส้นเชื่อมหลักของผัง SOP (ช้าๆ ไม่เร่ง) ให้เห็นทิศทาง "การเดินทางของข้อมูล" ชัดเจนขึ้น และแยกสีเขียว (เงื่อนไขผ่าน) / แดง (เงื่อนไขไม่ผ่าน-ถูกบล็อก) ให้จุดตัดสินใจที่มีทางบล็อก/ปฏิเสธจริงในผังงานแจ้งซ่อม, ยืม-คืนครุภัณฑ์, คลังอะไหล่
- แก้ไอคอนคำอธิบายสัญลักษณ์ในผัง SOP ที่หายไป (ขอบเส้นไม่มี border-style เพราะโปรเจกต์นี้ปิด Tailwind preflight) พร้อมเติมสีพื้นอ่อนให้แต่ละสัญลักษณ์แยกจากกันชัดเจนขึ้น
- แยกแบบฟอร์มพิมพ์ใบแจ้งซ่อมเป็น 2 แบบ: ครุภัณฑ์คอมพิวเตอร์/อุปกรณ์คอมพิวเตอร์ยังใช้แบบฟอร์มเดิม ส่วนเครื่องปรับอากาศ/ครุภัณฑ์การแพทย์/ครุภัณฑ์อื่นๆ ใช้แบบฟอร์ม "ใบแจ้งซ่อม" ของงานพัสดุ (ตามแบบฟอร์มกระดาษต้นแบบ) แทนโดยอัตโนมัติตามหมวดหมู่ครุภัณฑ์ — เพิ่มแบบฟอร์มเอกสารใหม่ REPAIR_REQUEST_GENERAL พร้อมเลขที่วิ่งอัตโนมัติ (RG-)
- ปรับผัง "ผังการทำงานระบบ (SOP)" ให้ดูทันสมัยขึ้น: เส้นเชื่อมลำดับขั้นตอนหลักมีจุดไหลเคลื่อนที่แสดงทิศทางข้อมูลจริง (ใช้ฟีเจอร์ animate ของ mermaid เอง เร่งความเร็วให้เห็นชัด), โหนดแต่ละขั้นทยอยปรากฏตอนโหลดหน้า, เอฟเฟกต์ hover ยกการ์ดขึ้นเบาๆ พร้อมเงาเมื่อชี้เมาส์
- เริ่มบันทึก Changelog นี้ทุกครั้งที่มีการแก้ไขโค้ด และผูกไว้กับการอัปเดตผัง "ผังการทำงานระบบ (SOP)" ให้ตรงกับระบบจริงเสมอ (ดู `CLAUDE.md`)
- เพิ่มเมนู "ผังการทำงานระบบ (SOP)" แสดงผังงานกระบวนการและการส่งข้อมูลของทุกระบบ (งานแจ้งซ่อม, ยืม-คืนครุภัณฑ์, ส่งซ่อมภายนอก, คลังอะไหล่, การแจ้งเตือน, ครุภัณฑ์ & QR, เอกสารราชการ, สิทธิ์การใช้งาน)
- ย้ายปุ่มค้นหาไปไว้ข้างปุ่มสลับโหมดมืด/สว่างในแถบด้านบน และปรับชื่อหน่วยงานให้ตัวหนา ตัวใหญ่ขึ้น เป็นสีเขียวตามธีม
- ปรับปุ่ม "ซ่อนเมนู" ให้ย่อเหลือแถบไอคอนแทนการซ่อนเมนูทั้งแผง (ยังเห็นป้ายแจ้งเตือนงานแจ้งซ่อมบนไอคอนได้) พร้อมแก้ช่องว่างที่เกิดขึ้นหลังย่อเมนู

## 2026-09-02

- จำกัดสิทธิ์การมองเห็น/แก้ไขงานแจ้งซ่อม: ผู้ใช้งานทั่วไปเห็นเฉพาะงานที่ตัวเองแจ้ง, ช่างเทคนิค/เจ้าหน้าที่ไอทีแก้ไขสถานะ/ผลตรวจ/สรุปผลได้เฉพาะงานที่แจ้งเองหรือได้รับมอบหมาย (แอดมิน/ผู้ดูแลระบบสูงสุดไม่จำกัด)
- บล็อกการแจ้งซ่อมซ้ำซ้อนต่อครุภัณฑ์เดียวกัน พร้อมแสดงเลขที่ใบเดิม/สถานะล่าสุดตอนสแกน QR
- เพิ่มการถ่ายภาพ/บันทึกวิดีโอจากกล้องเครื่องจริงในฟอร์มแจ้งซ่อม (ใช้ getUserMedia แทนตัวเลือกไฟล์ที่ใช้ไม่ได้บนเดสก์ท็อป) จำกัดรวมไม่เกิน 10 MB พร้อมแสดงขนาดไฟล์แต่ละรายการ
- แก้บั๊กวิดีโอที่บันทึกจากกล้องถูก backend ปฏิเสธเพราะ mimetype ที่มี codec parameter ทำให้ parser อ่านผิด
- เปลี่ยน QR Code ที่พิมพ์ให้เข้ารหัสเป็น URL แบบสั้นแทน token ยาวเดิม
- แสดงรูปครุภัณฑ์ (หรือไอคอนหมวดหมู่ถ้าไม่มีรูป) และรายละเอียดเพิ่มเติมในหน้าสแกน QR และหน้ารายละเอียดใบแจ้งซ่อม
- เพิ่มตัวเลขแจ้งเตือนงานแจ้งซ่อมใหม่ที่ยังไม่มีคนดูที่เมนู sidebar พร้อมบันทึกผู้เข้าดูคนแรก
- ให้สิทธิ์ ticket:create แก่ role เจ้าหน้าที่ไอทีและช่างเทคนิคเพิ่มเติม
- ขยายการแสดงผลข้อมูลที่ซิงค์จาก MOPH AssetTracker ในตารางครุภัณฑ์ (แถวขยายได้, คอลัมน์ equip_sub_name, ฟิลด์ที่เคยไม่มีที่เก็บ)
- ลบ Angular Service Worker ที่ทำให้เกิด console error NG05604
- ปรับข้อความ UX หน้าตั้งค่า PIN ให้ชัดเจนขึ้นว่า "ลืมอุปกรณ์"/"ปิดใช้งานทั้งหมด" ต้องตั้ง PIN ใหม่ทั้งหมด (ไม่ใช่บั๊ก)

## 2026-09-01

- ย้ายพอร์ต dev frontend จาก 4500 เป็น 4600 กันชนกับพอร์ตของโปรเจกต์อื่นในเครื่องเดียวกัน

## 2026-08-31

- เพิ่มฟีเจอร์นำเข้า/ซิงค์ครุภัณฑ์จาก MOPH AssetTracker อัตโนมัติทุกวัน 02:00 น. พร้อมปุ่มนำเข้าเองในหน้าแอดมิน
- เพิ่มปุ่มสแกน QR ด้วยกล้องในตัวที่หน้า login
- เปิดใช้ HTTPS ที่ Nginx reverse proxy เพื่อให้กล้อง/สแกน QR ทำงานผ่าน LAN IP ได้ (browser บล็อก getUserMedia บน HTTP)

## 2026-08-30

- เพิ่มระบบ PIN 6 หลักผูกกับอุปกรณ์สำหรับเข้าสู่ระบบด่วน พร้อมแก้ไขหลายจุด (การปิด/เปิดใช้งานใหม่, ข้อมูลผู้ใช้อื่นรั่วไหลบนเครื่องที่ใช้ร่วมกัน, สไตล์หน้าจอให้โปร่งใสตามธีม)
- ปรับ rate limit ให้เหมาะกับ Docker Desktop ที่ NAT รวม client หลายเครื่องเป็น IP เดียว
